package Program;

import java.math.BigInteger;

public class EstimateNConfig {
    // how many loops to run (max of n as in 2^n)
    public static int MaxNLoops = 80;
    // max value bit size
    public static int MaxInputBits = 80;
    // socket port for OT
    public static int socketPort = 23456;
}